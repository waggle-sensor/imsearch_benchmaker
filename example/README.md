# Example

This folder contains an example of how to use the imsearch_benchmaker framework with its inputs and outputs.

>NOTE: The files do not contain all the data, they are just examples. If you run the full pipeline with the provided config.toml file, you will get the full input and output files in the respective directories and you will find the .arrow files in the hf_dataset directory.